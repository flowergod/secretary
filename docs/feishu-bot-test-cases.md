# 飞书机器人测试用例文档

**创建日期**: 2026-05-08
**更新日期**: 2026-05-08
**测试文件**: `tests/unit/feishu-bot-service.test.ts`
**测试数量**: 136

---

## 测试用例总览

| 序号 | 测试组 | 用例数 | TC 编号 |
|------|--------|--------|---------|
| 1 | 消息处理 | 2 | TC-001 ~ TC-002 |
| 2 | 上下文管理 | 18 | TC-003 ~ TC-020 |
| 3 | 语义理解集成 | 3 | TC-021 ~ TC-023 |
| 4 | 消息回复 | 5 | TC-024 ~ TC-028 |
| 5 | 错误处理 | 2 | TC-029 ~ TC-030 |
| 6 | 欢迎语 | 2 | TC-031 ~ TC-032 |
| 7 | 长连接模式 | 3 | TC-033 ~ TC-035 |
| 8 | 单聊群聊支持 | 3 | TC-036 ~ TC-038 |
| 9 | 定时推送 | 4 | TC-039 ~ TC-042 |
| 10 | 飞书事件结构 | 2 | TC-043 ~ TC-044 |
| 11 | 意图类型覆盖 | 25 | TC-045 ~ TC-050 |
| 12 | 参数提取 | 8 | TC-051 ~ TC-054 |
| 13 | 置信度边界 | 6 | TC-055 ~ TC-056 |
| 14 | 复杂场景 | 7 | TC-057 ~ TC-059 |
| 15 | 多轮对话 | 12 | TC-060 ~ TC-062 |
| 16 | 任务类型 | 11 | TC-063 ~ TC-067 |
| 17 | 错误场景 | 7 | TC-068 ~ TC-070 |
| 18 | 集成场景 | 5 | TC-071 ~ TC-072 |

---

## 测试组 1: 消息处理

### TC-001 ~ TC-002

| ID | 用例名称 | 输入 | 预期结果 |
|----|---------|------|---------|
| TC-001 | 接收并解析飞书消息 | `MockFeishuEvent` | 正确解析 `text`, `chatId`, `openId` |
| TC-002 | 支持文本消息类型 | `text: "今天有什么安排"` | `typeof text === "string"` |

---

## 测试组 2: 上下文管理

### 2.1 shouldStartNewContext (TC-003 ~ TC-014)

| ID | 用例名称 | 输入 | 预期结果 |
|----|---------|------|---------|
| TC-003 | 首次对话开启新上下文 | `existingContextId = null` | `shouldNew === true` |
| TC-004 | completed 状态开启新上下文 | `status: 'completed'` | `shouldNew === true` |
| TC-005 | cancelled 状态开启新上下文 | `status: 'cancelled'` | `shouldNew === true` |
| TC-006 | expired 状态开启新上下文 | `status: 'expired'` | `shouldNew === true` |
| TC-007 | pending_confirmation 不开启新上下文 | `status: 'pending_confirmation'` | `shouldNew === false` |
| TC-008 | executing 不开启新上下文 | `status: 'executing'` | `shouldNew === false` |
| TC-009 | "新对话"关键词 | `"新对话"` | 开启新上下文 |
| TC-010 | "重新开始"关键词 | `"重新开始"` | 开启新上下文 |
| TC-011 | "换个话题"关键词 | `"换个话题"` | 开启新上下文 |
| TC-012 | "/new"命令 | `"/new"` | 开启新上下文 |
| TC-013 | "/reset"命令 | `"/reset"` | 开启新上下文 |
| TC-014 | 普通对话不开启新上下文 | `"提醒我明天上午9点开会"` | `shouldNew === false` |

### 2.2 contextId 生成 (TC-015 ~ TC-017)

| ID | 用例名称 | 输入 | 预期结果 |
|----|---------|------|---------|
| TC-015 | chatId 作为标识一部分 | `chatId = "oc_12345"` | `contextId.includes(chatId)` |
| TC-016 | 不同 chatId 生成不同 contextId | `oc_123`, `oc_456` | `contextId1 !== contextId2` |
| TC-017 | 前缀为 "feishu_" | `"feishu_" + chatId` | `startsWith("feishu_")` |

### 2.3 多会话支持 (TC-018 ~ TC-019)

| ID | 用例名称 | 输入 | 预期结果 |
|----|---------|------|---------|
| TC-018 | 每个 chatId 独立维护 contextId | 两个不同用户 | `contextA.id !== contextB.id` |
| TC-019 | 群聊和单聊 contextId 不同 | `p2pChatId`, `groupChatId` | `contextIdP2P !== contextIdGroup` |

### 2.4 上下文状态流转 (TC-020)

| ID | 用例名称 | 状态列表 | 预期结果 |
|----|---------|---------|---------|
| TC-020 | 包含所有定义的状态 | `pending_confirmation, executing, completed, cancelled, expired` | 包含全部5种状态 |

---

## 测试组 3: 语义理解集成 (TC-021 ~ TC-023)

| ID | 用例名称 | 输入 | 预期结果 |
|----|---------|------|---------|
| TC-021 | 调用 semanticService.understand | `"创建一个测试任务"` | `understand(text, contextId)` 被调用 |
| TC-022 | 需要确认时生成确认问题 | 低置信度 `0.5` | `confirmationQuestion !== undefined` |
| TC-023 | 语义理解成功返回任务信息 | `"提醒我明天上午9点开会"` | `result.success === true` |

---

## 测试组 4: 消息回复 (TC-024 ~ TC-028)

| ID | 用例名称 | 输入 | 预期结果 |
|----|---------|------|---------|
| TC-024 | 发送文本消息 | `"已为您创建任务"` | `typeof message === "string"` |
| TC-025 | 回复包含 openId | `openId = "ou_456"` | `openId === "ou_456"` |
| TC-026 | 支持卡片消息格式 | `card` 对象 | `msg_type === "interactive"` |
| TC-027 | 确认卡片包含选项按钮 | 确认卡片结构 | `elements.length >= 2` |
| TC-028 | 日程查询卡片显示日期 | 日程卡片 | `title.content.includes("今日日程")` |

---

## 测试组 5: 错误处理 (TC-029 ~ TC-030)

| ID | 用例名称 | 输入 | 预期结果 |
|----|---------|------|---------|
| TC-029 | 语义理解失败返回错误 | LLM 不可用 | `result.success === false` |
| TC-030 | 未知意图回复友好错误信息 | 模糊输入 | 包含 "抱歉" 和 "创建任务" |

---

## 测试组 6: 欢迎语 (TC-031 ~ TC-032)

| ID | 用例名称 | 内容 | 预期结果 |
|----|---------|------|---------|
| TC-031 | 首次消息欢迎语 | "您好！我是秘书小助手" | 包含 "秘书小助手" |
| TC-032 | 欢迎语引导用户开始对话 | "请说「新对话」" | 包含 "新对话" |

---

## 测试组 7: 长连接模式 (TC-033 ~ TC-035)

| ID | 用例名称 | 配置 | 预期结果 |
|----|---------|------|---------|
| TC-033 | 支持 WebSocket 长连接 | `connectionMode = "websocket"` | `connectionMode === "websocket"` |
| TC-034 | 自动处理重连 | `autoReconnect = true` | `autoReconnect === true` |
| TC-035 | 正确配置事件订阅 | `["im.message.receive_v1"]` | 包含消息接收事件 |

---

## 测试组 8: 单聊群聊支持 (TC-036 ~ TC-038)

| ID | 用例名称 | 输入 | 预期结果 |
|----|---------|------|---------|
| TC-036 | 区分单聊和群聊 | `chatType: 'p2p'` vs `'group'` | `p2p !== group` |
| TC-037 | 单聊回复给用户 openId | `chatType: 'p2p'` | `receiveId === openId` |
| TC-038 | 群聊回复给群 chatId | `chatType: 'group'` | `receiveId === chatId` |

---

## 测试组 9: 定时推送 (TC-039 ~ TC-042)

| ID | 用例名称 | 配置 | 预期结果 |
|----|---------|------|---------|
| TC-039 | 支持 cron 表达式 | `"0 9 * * 1-5"` | `cron === "0 9 * * 1-5"` |
| TC-040 | 支持多种通知类型 | `daily_morning, weekly_summary` | 包含对应类型 |
| TC-041 | 支持启用/禁用切换 | `enabled: true` | `enabled === true` |
| TC-042 | 支持设置推送时间 | 早上8点，晚上20点 | cron 包含对应时间 |

---

## 测试组 10: 飞书事件结构 (TC-043 ~ TC-044)

| ID | 用例名称 | 输入 | 预期结果 |
|----|---------|------|---------|
| TC-043 | 支持飞书消息事件格式 | `feishuEvent` 对象 | `event_type === "im.message.receive_v1"` |
| TC-044 | 解析消息内容 JSON | `'{"text":"提醒我..."}'` | `parsed.text === "提醒我..."` |

---

## 测试组 11: 意图类型覆盖 (TC-045 ~ TC-050)

### CREATE_TASK (6用例)

| ID | 用例名称 | 输入 |
|----|---------|------|
| TC-045-01 ~ TC-045-06 | 创建任务意图识别 | `"创建一个任务"`, `"帮我安排明天上午9点开会"`, `"提醒我今天下班去超市"`, `"新建一个待办"`, `"添加一个任务"`, `"安排明天下午3点拜访客户"` |

### QUERY_TASKS (5用例)

| ID | 用例名称 | 输入 |
|----|---------|------|
| TC-046-01 ~ TC-046-05 | 查询任务意图识别 | `"我有哪些待办任务"`, `"查看任务列表"`, `"今天的任务有哪些"`, `"还有什么没完成的"`, `"我的任务"` |

### QUERY_EVENTS (5用例)

| ID | 用例名称 | 输入 |
|----|---------|------|
| TC-047-01 ~ TC-047-05 | 查询日程意图识别 | `"今天有什么安排"`, `"今天怎么样"`, `"明天有什么日程"`, `"查看这周的日程"`, `"今天的会议有哪些"` |

### UPDATE_TASK (4用例)

| ID | 用例名称 | 输入 |
|----|---------|------|
| TC-048-01 ~ TC-048-04 | 更新任务意图识别 | `"把会议改到下午3点"`, `"帮我修改一下任务时间"`, `"调整任务优先级"`, `"更新任务描述"` |

### COMPLETE_TASK (5用例)

| ID | 用例名称 | 输入 |
|----|---------|------|
| TC-049-01 ~ TC-049-05 | 完成任务意图识别 | `"项目计划已经写完了"`, `"完成任务"`, `"标记为完成"`, `"搞定了"`, `"任务完成了"` |

### DELETE_TASK (4用例)

| ID | 用例名称 | 输入 |
|----|---------|------|
| TC-050-01 ~ TC-050-04 | 删除任务意图识别 | `"删除那个任务"`, `"把体检提醒删了"`, `"取消这个任务"`, `"删掉会议"` |

---

## 测试组 12: 参数提取 (TC-051 ~ TC-054)

### 日期时间提取 (TC-051)

| ID | 用例名称 | 输入 | 预期结果 |
|----|---------|------|---------|
| TC-051-01 | "今天" 为当前日期 | `new Date()` | `YYYY-MM-DD` 格式 |
| TC-051-02 | "明天" 为明天日期 | `Date + 1` | `YYYY-MM-DD` 格式 |
| TC-051-03 | "上午9点" 提取 | `"上午9点"` | 正则匹配 `上午(\d+)点` |
| TC-051-04 | "下午3点" 提取 | `"下午3点"` | 正则匹配 `下午(\d+)点` |
| TC-051-05 | "YYYY-MM-DD" 格式 | `"2026-05-15"` | 正则匹配日期格式 |

### 优先级提取 (TC-052)

| ID | 用例名称 | 映射 |
|----|---------|------|
| TC-052 | "高优先级/紧急" → `high`, "普通" → `medium` | 标准化 |

### 分类提取 (TC-053)

| ID | 用例名称 | 映射 |
|----|---------|------|
| TC-053 | "工作/个人/家庭" | 标准化 |

---

## 测试组 13: 置信度边界 (TC-054 ~ TC-056)

### 置信度阈值 (TC-054)

| ID | 用例名称 | 输入 | 预期结果 |
|----|---------|------|---------|
| TC-054-01 | 高置信度 >= 0.8 | `confidence = 0.95` | 直接执行，不需要确认 |
| TC-054-02 | 低置信度 0.5-0.7 | `confidence = 0.6` | 需要确认 |
| TC-054-03 | 模糊输入 < 0.5 | `confidence = 0.3` | 需要确认 |

### 确认触发条件 (TC-055)

| ID | 用例名称 | 条件 | 预期结果 |
|----|---------|------|---------|
| TC-055-01 | 高置信度不需要确认 | `confidence = 0.95` | `needsConfirmation = false` |
| TC-055-02 | 低置信度需要确认 | `confidence = 0.5` | `needsConfirmation = true` |
| TC-055-03 | 缺少必填参数需要确认 | 缺少 `title` | `needsConfirmation = true` |

---

## 测试组 14: 复杂场景 (TC-056 ~ TC-058)

### 循环任务识别 (TC-056)

| ID | 用例名称 | 输入 | 预期结果 |
|----|---------|------|---------|
| TC-056-01 | 每周循环 | `"每周一早上9点开会"` | `recurrence_type = "weekly"` |
| TC-056-02 | 每天循环 | `"每天早上跑步"` | `recurrence_type = "daily"` |
| TC-056-03 | 每月循环 | `"每月15号交房租"` | `recurrence_type = "monthly"` |
| TC-056-04 | is_recurring=true | 循环任务 | `is_recurring === true` |

### 时间段识别 (TC-057)

| ID | 用例名称 | 输入 | 预期结果 |
|----|---------|------|---------|
| TC-057-01 | 上午时间 | `"上午11点"` | 识别为上午 |
| TC-057-02 | 下午时间 | `"下午3点"` | 识别为下午 |
| TC-057-03 | 晚上时间 | `"晚上8点"` | 识别为晚上 |

### 模糊时间处理 (TC-058)

| ID | 用例名称 | 输入 | 预期结果 |
|----|---------|------|---------|
| TC-058 | "稍后/晚点" | `"稍后提醒我"` | 触发确认 |

---

## 测试组 15: 多轮对话 (TC-059 ~ TC-062)

### 多轮对话续 (TC-059)

| ID | 用例名称 | 状态 | 预期结果 |
|----|---------|------|---------|
| TC-059-01 | 确认状态复用 | `pending_confirmation` | `shouldReuse = true` |
| TC-059-02 | 执行状态复用 | `executing` | `shouldReuse = true` |
| TC-059-03 | 完成后开启新上下文 | `completed` | `shouldReuse = false` |

### 确认选项处理 (TC-060)

| ID | 用例名称 | 输入 | 预期结果 |
|----|---------|------|---------|
| TC-060-01 | 选项编号 | `"opt_1"` | 正则匹配 `^opt_(\d+)$` |
| TC-060-02 | 中文选项 | `"选项1"` | 正则匹配 `^(?:选项)?(\d+)$` |
| TC-060-03 | 纯数字 | `"1"` | 正则匹配 `^(\d+)$` |
| TC-060-04 | 确认关键词 | `"好的/是/确认"` | 识别为确认 |
| TC-060-05 | 取消关键词 | `"算了/取消"` | 识别为取消 |

### 上下文过期 (TC-061)

| ID | 用例名称 | 条件 | 预期结果 |
|----|---------|------|---------|
| TC-061-01 | 超时后过期 | `expiresAt < now` | `isExpired = true` |
| TC-061-02 | 有效上下文不过期 | `expiresAt > now` | `isExpired = false` |

### 状态转换 (TC-062)

| ID | 用例名称 | 转换 | 预期结果 |
|----|---------|------|---------|
| TC-062-01 | pending → executing | `pending_confirmation` → `executing` | 有效转换 |
| TC-062-02 | executing → completed | `executing` → `completed` | 有效转换 |

---

## 测试组 16: 任务类型 (TC-063 ~ TC-067)

### 普通任务 (TC-063)

| ID | 用例名称 | 预期结果 |
|----|---------|---------|
| TC-063-01 | 创建普通任务 | `is_recurring = false` |

### 循环任务 (TC-064)

| ID | 用例名称 | 预期结果 |
|----|---------|---------|
| TC-064-01 ~ TC-064-04 | daily/weekly/monthly/yearly | `recurrence_type` 正确 |
| TC-064-05 | RRULE 格式 | `recurrence_rule` 包含 `FREQ=WEEKLY` |

### 日程任务 (TC-065)

| ID | 用例名称 | 预期结果 |
|----|---------|---------|
| TC-065-01 | start_date 存在 | 视为日程 |
| TC-065-02 | 支持 end_time | `end_time` 存在 |

### 任务状态 (TC-066)

| ID | 用例名称 | 状态列表 |
|----|---------|---------|
| TC-066-01 ~ TC-066-04 | pending/in_progress/completed/cancelled | 全部支持 |

### 任务优先级 (TC-067)

| ID | 用例名称 | 优先级列表 |
|----|---------|---------|
| TC-067-01 ~ TC-067-03 | high/medium/low | 全部支持 |

---

## 测试组 17: 错误场景 (TC-068 ~ TC-070)

### 输入验证错误 (TC-068)

| ID | 用例名称 | 输入 | 预期结果 |
|----|---------|------|---------|
| TC-068-01 | 空消息被拒绝 | `""` | `isValid = false` |
| TC-068-02 | 空白字符被拒绝 | `"   "` | `isValid = false` |

### LLM 错误处理 (TC-069)

| ID | 用例名称 | 错误类型 | 预期结果 |
|----|---------|---------|---------|
| TC-069-01 | LLM 超时 | `"服务响应超时，请稍后重试"` | 包含 "超时" |
| TC-069-02 | LLM 限流 | `"服务繁忙，请稍后再试"` | 包含 "繁忙" 或 "稍后" |

### 数据错误处理 (TC-070)

| ID | 用例名称 | 错误类型 | 预期结果 |
|----|---------|---------|---------|
| TC-070-01 | 任务不存在 | `"未找到指定的任务"` | 包含 "未找到" |
| TC-070-02 | 重复创建 | `"该任务已存在"` | 包含 "已存在" |

---

## 测试组 18: 集成场景 (TC-071 ~ TC-072)

### 完整对话流程 (TC-071)

| ID | 用例名称 | 流程 | 预期结果 |
|----|---------|------|---------|
| TC-071-01 | 创建任务完整流程 | 消息 → 意图 → 参数 → 执行 → 结果 | 全流程成功 |
| TC-071-02 | 查询日程完整流程 | 消息 → QUERY_EVENTS → 返回日程 | 全流程成功 |
| TC-071-03 | 需要确认的流程 | 低置信度 → 确认问题 → 用户确认 → 执行 | 流程正确 |

### 边界场景 (TC-072)

| ID | 用例名称 | 输入 | 预期结果 |
|----|---------|------|---------|
| TC-072-01 | 极长消息截断 | 2000字符 | 截断到 1000 字符 |
| TC-072-02 | 特殊字符转义 | `"<>&"` | 转义为 HTML 实体 |

---

## 执行记录

| 日期 | 测试数 | 通过 | 失败 | 状态 |
|------|--------|------|------|------|
| 2026-05-08 | 136 | 136 | 0 | ✅ |

---

## 新增测试用例

### 意图类型覆盖 (TC-045 ~ TC-050)
- CREATE_TASK: 6个测试用例
- QUERY_TASKS: 5个测试用例
- QUERY_EVENTS: 5个测试用例
- UPDATE_TASK: 4个测试用例
- COMPLETE_TASK: 5个测试用例
- DELETE_TASK: 4个测试用例

### 参数提取 (TC-051 ~ TC-054)
- 日期时间提取: 5个测试用例
- 优先级提取: 1个测试用例
- 分类提取: 1个测试用例

### 置信度边界 (TC-055 ~ TC-056)
- 置信度阈值: 3个测试用例
- 确认触发条件: 3个测试用例

### 复杂场景 (TC-057 ~ TC-058)
- 循环任务识别: 4个测试用例
- 时间段识别: 3个测试用例
- 模糊时间处理: 1个测试用例

### 多轮对话 (TC-059 ~ TC-062)
- 多轮对话续: 3个测试用例
- 确认选项处理: 5个测试用例
- 上下文过期: 2个测试用例
- 状态转换: 2个测试用例

### 任务类型 (TC-063 ~ TC-067)
- 普通任务: 1个测试用例
- 循环任务: 5个测试用例
- 日程任务: 2个测试用例
- 任务状态: 4个测试用例
- 任务优先级: 3个测试用例

### 错误场景 (TC-068 ~ TC-070)
- 输入验证错误: 2个测试用例
- LLM 错误处理: 2个测试用例
- 数据错误处理: 2个测试用例

### 集成场景 (TC-071 ~ TC-072)
- 完整对话流程: 3个测试用例
- 边界场景: 2个测试用例
